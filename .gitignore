# ------------------------------------------------------
# 1. Dependency Directories
# ------------------------------------------------------
node_modules/
pnpm-lock.yaml
yarn.lock
package-lock.json
# ^ Keep lockfiles if you want deterministic builds (Recommended), 
# comment them out above if you want to commit them (Standard practice).

# ------------------------------------------------------
# 2. Build Outputs (TypeScript)
# ------------------------------------------------------
dist/
build/
coverage/
*.tsbuildinfo

# ------------------------------------------------------
# 3. Environment Variables (CRITICAL)
# ------------------------------------------------------
.env
.env.production
.env.test
.env.local

# ------------------------------------------------------
# 4. Database & Drizzle ORM
# ------------------------------------------------------
# Ignore local SQLite databases if you use them for dev
*.sqlite
*.db
*.sqlite-journal

# OPTIONAL: Ignore generated SQL migrations if you don't want them in Git.
# Standard practice is to KEEP them in git so you can migrate production.
# drizzle/

# ------------------------------------------------------
# 5. Docker & System
# ------------------------------------------------------
.docker/
docker-data/
postgres-data/
.DS_Store
Thumbs.db

# ------------------------------------------------------
# 6. IDE & Editor Configs
# ------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo

# ------------------------------------------------------
# 7. Logs
# ------------------------------------------------------
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*